import 'package:flutter/material.dart';

class AppColors {
  static const Color background = Color(0xFF0A0E21);
  static const Color cardColor = Color(0xFF1D1E33);
  static const Color primary = Color(0xFF6C63FF);
  static const Color accent = Color(0xFF03DAC6);
  static const Color textPrimary = Colors.white;
  static const Color textSecondary = Colors.white70;
}

class AppStyles {
  static const EdgeInsets pagePadding = EdgeInsets.symmetric(horizontal: 40, vertical: 20);
  static const EdgeInsets sectionPadding = EdgeInsets.symmetric(vertical: 60, horizontal: 20);
}
